---
- name: Hello World on localhost
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Say hello
      ansible.builtin.debug:
        msg: "Hello from Ansible on {{ inventory_hostname }}!"
